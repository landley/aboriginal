# Mips r4k little endian

KARCH=mips
KERNEL_PATH=vmlinux
BINUTILS_FLAGS=
GCC_FLAGS=
QEMU_TEST=mipsel

ROOT=hda
CONSOLE=ttyS0

emulator_command()
{
  echo qemu-system-mipsel -M malta $(qemu_defaults "$@")
}
